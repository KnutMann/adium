/*
 * XtrasCreator is part of the Adium project and shares its license; see the
 * License.txt at the repository root.
 */

#import "AXSEditorViewController.h"

/*!
 * @brief The type page for icon packs organized in categories
 *
 * One card per category (List, Tabs, the interface sizes, ...), each a table
 * of the keys the format knows plus whatever else the pack carries. Keys the
 * pack must have are marked when empty; keys of long-gone services stay and
 * are labelled as kept.
 */
@interface AXSIconPackEditorViewController : AXSEditorViewController
@end
