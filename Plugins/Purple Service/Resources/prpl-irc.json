{
	"note": [
		"IRC declares no server option: the server is part of the account name, nick@irc.example.net,",
		"which ESIRCAccount puts together. The account card therefore asks for one even though the",
		"protocol does not name it."
	],

	"commandsOnConnect": true,

	"account": {
		"server": true,
		"port": true,
		"options": ["ssl"]
	},

	"legacy": {
		"IRC:Use SSL": "ssl",
		"IRC:Username": "username",
		"IRC:Realname": "realname",
		"IRC:Encoding": "encoding"
	}
}
