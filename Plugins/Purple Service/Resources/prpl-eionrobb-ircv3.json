{
	"note": [
		"As prpl-irc, but bound without any code of its own: the server goes into the account name,",
		"which the descriptor states with AccountNameIncludesHost.",
		"",
		"Nothing is promoted beyond what it takes to connect. The ident name and the real name read as",
		"a second account name and a second display name, and neither is what anybody came here for."
	],

	"commandsOnConnect": true,

	"account": {
		"server": "inAccountName",
		"port": true,
		"options": ["ssl"]
	}
}
